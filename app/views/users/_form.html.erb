<%= form_with(model: user) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :full_name %>
    <%= form.text_field :full_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address %>
  </div>

  <div class="field">
    <%= form.label :mobile_no %>
    <%= form.number_field :mobile_no %>
  </div>

  <div class="field">
    <%= form.label :pin_code %>
    <%= form.number_field :pin_code %>
  </div>

<!--   <div class="field">
    <%#= form.label :user_type %>
    <%#= form.number_field :user_type %>
    <%#= form.select(:user_type, ["sender", "receiver"], {}, { :class => 'my_style_class' }) %>
  </div>
 -->
  <div class="actions">
    <%= form.submit "Save Details" %>
  </div>
<% end %>
